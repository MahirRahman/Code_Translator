<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <link href="public/styles.css" type="text/css" rel="stylesheet" />
    <title>History Page</title>
  </head>
  <body>
    <div id="nav">
        <a href="./"><button> Home </button></a>
        <a href="./history"><button> History </button></a>
        <form action="/logout" method="post">
            <button class="logout" type="submit">Log Out</button>
        </form>
    </div>
    <br>
    <h1 align="center">History</h1>
    <div
      class="history-table text-center table-responsive container h-100 d-flex align-items-center justify-content-center"
    >
      <table class="table table-bordered table-sm align-middle table-striped">
        <thead>
          <tr>
            <th scope="col">User</th>
            <th scope="col">Input</th>
            <th scope="col">Translation</th>
            <th scope="col">Date</th>
          </tr>
        </thead>

        <tr>
          <% data.map((entry)=> { %>
          <td><%= entry.id %></td>
          <td><%= entry.searchText %></td>
          <td><%= entry.result %></td>
          <td><%= entry.date %></td>
        </tr>
        <% }) %>
      </table>
    </div>
  </body>
</html>
